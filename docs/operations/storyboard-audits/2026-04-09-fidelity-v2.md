# Storyboard Fidelity Audit — Part A (v2)

- **Date:** 2026-04-09 (second pass)
- **Auditor:** GM (Product & Ops) agent
- **Method:** Code reading + `curl` against http://localhost:5174 (`/`, `/today`, `/members` all 200)
- **Rubric:** PRODUCT-FINALIZATION.md §10b Part A
- **Prior audit:** `docs/operations/storyboard-audits/2026-04-09-fidelity.md`

## Per-story scores (vs v1)

| Story | v1 | v2 | Delta |
|---|---|---|---|
| 1 — Saturday Briefing | 5 | **5** | — |
| 2 — Quiet Resignation Catch | 4 | **5** | **+1** |
| 3 — Revenue Leakage Discovery | 5 | **5** | — |

### Story 2 — verified fixes (commit 781486e)

1. **Dollar anchor chip in decay-chain card.** `MemberDecayChain.jsx:267-295` reads `profile.duesAnnual ?? dues_annual ?? dues ?? memberValueAnnual`, renders `$XXK/yr at risk` pill inside the same card header as the "First signal: N days ago" counter. Renders in both `drawer` and `page` variants. Never invents a number (guarded by `!duesAnnual || <=0`).
2. **Relative demo dates.** `MemberDecayChain.jsx:75-86` — hardcoded `'Oct 2025'`–`'Jan 2026'` replaced with `'~12 weeks ago'`, `'~8 weeks ago'`, `'~4 weeks ago'`, `'this week'`. Ages gracefully on any import window.
3. **Launcher anchor.** `MorningBriefingSentence.jsx:176` now carries `id="today-briefing" data-story="briefing"`; `DemoStoriesLauncher.jsx:79-82` prefers id, then data-attr, then `.fade-in-up` fallback.

All three v1 friction points are closed. Story 2 scores **5**.

## Criterion 10 verdict — **GO (clean)**

All three paths ≥ 5. The one-line narrator caveat from v1 is gone.

## NEW frictions introduced since v1

- **Duplicate dollar anchor in drawer + page.** `MemberProfileDrawer.jsx:723-735` and `MemberProfilePage.jsx:411-423` already render `$XXK/yr at risk` next to the health-score gauge. The new chip in `MemberDecayChain.jsx:286-295` now repeats the *same* number ~150px away. Cosmetic but reads as accidental duplication on a demo.
- **"First signal: N days ago" counter silently disappears on demo-fallback members.** `MemberDecayChain.jsx:240-247` calls `Date.parse('~12 weeks ago 1')` which is `NaN`, so `daysSinceFirstSignal` is `null` and the counter is not rendered. On a fresh pilot the chain shows steps but the temporal "how long this has been decaying" beat is lost.

## Audit file

`C:/GIT/Development/swoop-member-portal/docs/operations/storyboard-audits/2026-04-09-fidelity-v2.md`

## Top recommended fixes (new, not from v1)

1. **De-dupe the dollar anchor in the drawer.** `MemberProfileDrawer.jsx:723-735` — when `MemberDecayChain` will render (score < 50 and duesAnnual present), suppress the header chip. Single dominant `$XXK/yr at risk` next to the decay viz is the storyboard beat; two chips dilute it.
2. **Restore "First signal" counter for demo-fallback chains.** `MemberDecayChain.jsx:75-86` — tag each demo event with a numeric `weeksAgo` (12/8/4/0) and compute `daysSinceFirstSignal` from that when `Date.parse` fails. Keeps the "this has been decaying for 12 weeks" narration beat alive on pilots without real decay history.
3. **Reconcile `$31/slow round` narrative** (carryover from v1 Story 3 feels-off #1) — still unaddressed. Add a one-line derivation tooltip on the Hole 12 panel (`RevenuePage.jsx` bottleneck drilldown) so CFO math lands: `"$31 = monthly pace loss / slow rounds/month"`.
4. **"vs last month" delta chip on Total Monthly Leakage KPI** — also still open from v1 Story 3. Even a mocked `+$420 vs Mar` chip honors the "board prep" trend-spotting framing.
5. **Guard Story 2 launcher against empty at-risk list with a louder fallback.** `DemoStoriesLauncher.jsx:84-89` navigates to `members?tab=at-risk` silently. On a freshly-imported club with zero at-risk members, that tab is empty and the story dies. Render a disabled-state hint ("Import activity data to unlock") instead of a broken click.
