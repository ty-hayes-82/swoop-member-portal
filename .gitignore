node_modules/
dist/
.env
.env.local
*.log
.DS_Store
-e 
# Local SQLite test database
oakmont_hills_jan2026.db
*.db
.npm-cache/

dist.locked-*
.env*.local
.env.vercel
.vercel
# Local-only dev overrides — never commit. Excluding api/* dirs from prod would
# break Google OAuth, cron jobs, concierge, and onboarding-agent (see Agent D
# audit). This file only exists to let `vercel dev` boot under the 128-function
# cap locally.
.vercelignore

# Local QA artifacts
tmp/
tmp_*.mjs

# Debug / capture artifacts (local-only)
debug*.png
vision-screenshots/
vision-manifest.jsonl
insights-capture.jsonl
test-results/
playwright-report/
.claude/worktrees/
